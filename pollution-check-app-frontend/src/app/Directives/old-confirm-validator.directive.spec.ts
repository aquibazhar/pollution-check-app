import { OldConfirmValidatorDirective } from './old-confirm-validator.directive';

describe('OldConfirmValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OldConfirmValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
