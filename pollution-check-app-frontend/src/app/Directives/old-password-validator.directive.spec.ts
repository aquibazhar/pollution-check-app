import { OldPasswordValidatorDirective } from './old-password-validator.directive';

describe('OldPasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OldPasswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
